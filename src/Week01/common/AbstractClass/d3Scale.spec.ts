// FIXME: could not find module 'd3'
// const d3 = require('d3');
// import * as d3 from 'd3';

describe('test d3-scale', () => {

  test('linear scale', () => {
//     const scale = d3.scale.linear().domain([0, 360]).range([0, 12]);
//     expect(scale).toEqual([]);
  })
});